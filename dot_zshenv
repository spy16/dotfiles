set editing-mode vi

# ================== aliases ===================
alias vim="nvim"
alias git-personal="git config --local user.name Shivaprasad; git config --local user.email shiv.ylp@gmail.com"
alias git-work="git config --local user.name Shivaprasad; git config --local user.email shiva@aaritya.com"

# general aliases
alias kc="kubectl"
alias kt="kubetail"
alias kx="kubectx"
alias vim="nvim"
alias py="python3"
alias ipy="ipython"

alias dc="docker compose"
alias dcps='docker compose ps'
alias dcup='docker compose up'
alias dclogs='docker compose logs -f'
alias dcstop='docker compose stop'
alias dcdown='docker compose down'
alias dcexec='docker compose exec'

alias yarn='echo "Did you mean pnpm?"'
alias pn='pnpm'
alias px='pnpx'

# ================== exports ===================
export SSH_KEY_PATH=~/.ssh/id_rsa

export GOPATH=~/Installed/go
export GOBIN=$GOPATH/bin

export JAVA_HOME=/usr/local/opt/openjdk

export PATH=$PATH:$GOPATH/bin

# Preferred editor for local and remote sessions
if [[ -n $SSH_CONNECTION ]]; then
  export EDITOR='nvim'
else
  export EDITOR='vim'
fi

